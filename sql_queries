'TOP 10 VIEW' :
        "CREATE VIEW TOP_10 AS
        SELECT NOMBRE_PLATO, AVG(CALIFICACION) AS PROMEDIO_RESEÑAS
        FROM RESEÑA
        GROUP BY NOMBRE_PLATO
        ORDER BY PROMEDIO_RESEÑAS DESC
        LIMIT 10"

